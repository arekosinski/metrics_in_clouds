[Unit]
Description=msg publisher
Requires=network.target
After=syslog.target network.target

[Service]
Type=simple
ExecStart=/usr/bin/python3 /home/pi/metrics_in_clouds/src/msg_export_pubsub.py
ExceStop=/bin/kill -HUP $MAINPID
User=pi
PIDFile=/tmp/iot_msg_publisher.pid

[Install]
WantedBy=multi-user.target